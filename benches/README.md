# benches/

Cross-crate criterion benchmarks that don't naturally fit in any single
crate. Per-crate benches live in their own crate's `benches/` directory.

See [../docs/BENCHMARKS.md](../docs/BENCHMARKS.md) for the full bench
strategy.
